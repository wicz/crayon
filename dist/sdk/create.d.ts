import { SharedState } from '../platform/shared-state';
import { Router } from "../platform/router";
export declare const create: (id?: string, _window?: Window, sharedState?: SharedState | undefined) => Router;
